import { {{#each validators}} {{name}}{{#unless @last}}, {{/unless}}{{/each}} } from 'class-validator';

{{#each dtos}}
export class {{name}} {
  {{#each properties}}
  {{#each validators}}
  {{name}}
  {{/each}}
  {{name}}: {{type}}
  
  {{/each}}
}

{{/each}}